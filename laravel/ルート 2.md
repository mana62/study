# ルート
- URLへ直接リダイレクト
```php
return redirect('/some/path');
```

- 直前のページへ戻る
```php
return back()->withInput();
// withInput() : フォームの入力内容をセッションに保持
```

- 外部URLへリダイレクト
```php
return redirect()->away('https://example.com');
```