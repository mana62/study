# いろいろなイベント

## マウス・ポインター系
| イベント          | 意味            | いつ使う？    | 例       |
| ------------- | ------------- | -------- | ------- |
| `click`       | 左クリック         | ボタン操作    | ボタンクリック |
| `dblclick`    | ダブルクリック       | 特殊操作     | ファイル開く  |
| `contextmenu` | 右クリック         | 右クリック禁止等 | 右クリック制御 |
| `mousedown`   | 押した瞬間         | ドラッグ開始   | スライダー   |
| `mouseup`     | 離した瞬間         | ドラッグ終了   | スライダー   |
| `mousemove`   | 動かした時         | 追従UI     | お絵描き    |
| `mouseover`   | 要素に入る         | hover演出  | メニュー    |
| `mouseout`    | 要素から出る        | hover解除  | メニュー    |
| `mouseenter`  | 要素に入る（子要素無視）  | 安定hover  | メニュー    |
| `mouseleave`  | 要素から出る（子要素無視） | 安定hover  | メニュー    |

👉 実務では pointer系が最強
```js
pointerdown
pointerup
pointermove
pointerenter
pointerleave

element.addEventListener('pointerdown', () => {
  // pointerdown: ポインター（マウスやタッチ）が押されたとき
});

element.addEventListener('pointerup', () => {
  // pointerup: ポインターが離されたとき（クリックやタップの終了）
});

element.addEventListener('pointermove', () => {
  // pointermove: ポインターが動いたとき（ドラッグやスワイプ中など）
});

element.addEventListener('pointerenter', () => {
  // pointerenter: ポインターが要素に入ったとき（hoverの開始）
});

element.addEventListener('pointerleave', () => {
  // pointerleave: ポインターが要素から出たとき（hoverの終了）
});
```
👉 **マウス・タッチ・ペン全部対応**

## タッチ系
| イベント         | 意味    |
| ------------ | ----- |
| `touchstart` | 指が触れた |
| `touchmove`  | 指が動いた |
| `touchend`   | 指が離れた |

👉 今は pointer 系で代用可能

## キーボード系
| イベント      | 意味    | 用途         |
| --------- | ----- | ---------- |
| `keydown` | 押した瞬間 | 入力・ショートカット |
| `keyup`   | 離した瞬間 | 確定処理       |

```js
document.addEventListener("keydown", e => {
  console.log(e.key);
});
```

## スクロール系
| イベント     | 意味     | 注意   |
| -------- | ------ | ---- |
| `wheel`  | ホイール回転 | 大量発生 |
| `scroll` | スクロール  | 超重い  |

👉 IntersectionObserver API 推奨

## フォーカス系
| イベント    | 意味       |
| ------- | -------- |
| `focus` | フォーカスされた |
| `blur`  | フォーカス外れた |

```js
input.addEventListener("focus", ...);
input.addEventListener("blur", ...);
```

## フォーム入力系
```js
| イベント     | 意味   | 発火タイミング  |
| -------- | ---- | -------- |
| `input`  | 入力中  | 1文字ごと    |
| `change` | 入力完了 | フォーカス外れ時 |
```
👉 リアルタイム処理 → input
👉 確定処理 → change

## コピー系
| イベント    | 意味   |
| ------- | ---- |
| `copy`  | コピー  |
| `cut`   | 切り取り |
| `paste` | 貼り付け |

```js
document.addEventListener("copy", e => {
  e.preventDefault(); // コピー禁止
});
```

## フォーム送信系
| イベント     | 意味     |
| -------- | ------ |
| `submit` | フォーム送信 |

```js
form.addEventListener("submit", e => {
  e.preventDefault(); // 送信止める
});
```
👉 Ajax通信・バリデーションで必須

## 通信・ページ状態系
| イベント               | 意味       |
| ------------------ | -------- |
| `online`           | ネット復帰    |
| `offline`          | ネット切断    |
| `visibilitychange` | タブ切替     |
| `hashchange`       | URL # 変更 |


## 端末センサー系
| イベント                | 意味   |
| ------------------- | ---- |
| `devicemotion`      | 加速度  |
| `deviceorientation` | 端末向き |

👉 ゲーム・AR系

## まとめ（実務でよく使うイベント）
| 順位 | イベント                                    |
| -- | --------------------------------------- |
| 1  | `click`                                 |
| 2  | `pointerdown / pointermove / pointerup` |
| 3  | `input`                                 |
| 4  | `submit`                                |
| 5  | `keydown`                               |
| 6  | `scroll`（代替: IntersectionObserver）      |
| 7  | `focus / blur`                          |
