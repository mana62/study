# 属性やCSS

## 属性（attribute）とは？
```js
<input type="text" id="a" class="box">
```
上記の
- type
- id
- class
👉 これら全部が「属性」

## 属性 → プロパティになる
ブラウザはHTMLを解析して：
👉 属性をヒントに、DOMのプロパティを作る

```js
<input type="text">
```
⬇️
```js
input.type === "text"
```
```js
<body id="main">
```
⬇️
```js
document.body.id === "main"
```

## でも「全部」がプロパティになるわけではない
```js
<body myattr="123">
```
```js
document.body.myattr  // undefined
```
👉 仕様で決められた属性だけがプロパティになる

## すべての属性を見る方法 → attributes
```js
element.attributes
```
👉 HTMLに書かれた全属性の一覧

## 属性操作の4メソッド
```js
element.getAttribute('name');       // 属性の値を取得する（例: hrefの中身など）
element.setAttribute('name', 'value'); // 属性を設定する（なければ追加、あれば上書き）
element.hasAttribute('name');       // 属性があるかどうかをtrue/falseで返す
element.removeAttribute('name');    // 属性を削除する
```

## 属性とプロパティは連動する
```js
el.id = "a"
```
⬇️
```js
el.getAttribute("id") === "a"
```
逆も同じ
👉 基本的に双方向で連動

## 独自属性の問題点
```js
<div myattr="123">
```
👉 将来のHTML仕様で衝突する可能性がある

## 安全な独自属性 → data-*
```js
<div data-id="10" data-user-name="taro">
```
HTML仕様の約束：
👉 data- は開発者専用、安全ゾーン*

## data-* のJSアクセス方法
```js
el.dataset.id
el.dataset.userName
```
ルール：
```js
data-user-name → dataset.userName
```
👉 ケバブケース → キャメルケース

## クラス操作（超重要）
2つの方法
①className（あまり使わない）
```js
el.className = "a b c"
```
👉 全部上書き、ミスりやすい

②classList（実務でよく使われる）
```js
el.classList.add("active")
el.classList.remove("active")
el.classList.toggle("active")
el.classList.contains("active")
```
👉 クラス操作はこれだけ覚えればOK

## style プロパティ（直接CSS操作）
```js
el.style
```
👉 HTMLの style="" に対応


```js
el.style.color = "red"
el.style.backgroundColor = "yellow"
el.style.width = "300px"
```
重要ルール：
- 値は必ず文字列
- 単位必須（px, %, em など）
- CSS名 → キャメルケース

## style は「style属性」しか見ない
```js
.red { color:red; }
```
```js
<div class="red"></div>
```
```js
div.style.color  // ""
```
👉 class / CSSファイルの影響は見えない

## 実際の最終CSSを見る → getComputedStyle
```js
getComputedStyle(el)
```
👉 最終的に適用されているすべてのCSS

含む：
- style
- class
- CSSファイル
- ブラウザ標準CSS

## 表示 / 非表示操作
```js
el.style.display = "none"   // 非表示
el.style.display = ""       // 表示
```

または

```js
el.hidden = true
el.hidden = false
```

## まとめ
HTML → 属性
DOM → プロパティ
操作 → JavaScript

| やりたいこと  | 使うもの                    |
| ------- | ----------------------- |
| 属性を見る   | attributes              |
| 属性操作    | get/set/removeAttribute |
| 安全な独自属性 | data-* / dataset        |
| クラス操作   | classList               |
| 見た目変更   | style                   |
| 最終CSS取得 | getComputedStyle        |


### 実務でよく使う
```js
el.classList.add()
el.classList.remove()
el.classList.toggle()
el.dataset.xxx
el.style.display = "none"
```